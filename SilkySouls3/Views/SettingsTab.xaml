<UserControl x:Class="SilkySouls3.Views.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
             Focusable="True"
             FocusManager.FocusedElement="{x:Null}"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <DataTemplate x:Key="HotkeyItemTemplate">
            <StackPanel Orientation="Horizontal" Margin="0,5">
                <TextBlock Text="{Binding DisplayName}"
                           Width="150"
                           VerticalAlignment="Center" />
                <TextBox Text="{Binding HotkeyText}"
                         Style="{StaticResource HotkeyTextBoxStyle}"
                         Tag="{Binding ActionId}"
                         GotFocus="HotkeyTextBox_GotFocus"
                         LostFocus="HotkeyTextBox_LostFocus"
                         KeyDown="HotkeyTextBox_KeyDown" />
            </StackPanel>
        </DataTemplate>
        <xcad:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
        <xcad:BoolToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Border BorderThickness="1" BorderBrush="#333" CornerRadius="3" Margin="5">
        <StackPanel Margin="10">
            <TextBlock Text="Options" FontWeight="Bold" Margin="0,0,0,10" />

            <Grid Margin="0,5,0,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150" />
                    <ColumnDefinition Width="Auto" MinWidth="150" />
                    <ColumnDefinition Width="Auto" MinWidth="150" />
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Column="0" Content="Enable Hotkeys"
                          IsChecked="{Binding IsEnableHotkeysEnabled}" />
                <CheckBox Grid.Row="0" Grid.Column="1" Content="Stutter Fix"
                          IsChecked="{Binding IsStutterFixEnabled}" />
                <CheckBox Grid.Row="0" Grid.Column="2" Content="Always On Top"
                          IsChecked="{Binding IsAlwaysOnTopEnabled}" />

                <CheckBox Grid.Row="1" Grid.Column="0" Content="Disable Menu Music"
                          IsChecked="{Binding IsDisableMenuMusicEnabled}"
                          Margin="0,10,0,0" />

                <Button Grid.Row="1" Grid.Column="1" Content="Activate On Launch"
                        Command="{Binding OpenActivateOnLaunchCommand}"
                        HorizontalAlignment="Left"
                        Padding="10,3"
                        Margin="0,10,0,0" />
            </Grid>

            <Grid Margin="0,10,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <CheckBox Content="Hotkey Enabled Reminder"
                              IsChecked="{Binding IsHotkeyReminderEnabled}" />
                    <Border Background="#3498db" CornerRadius="10" Width="12" Height="12"
                            Margin="5,0,0,0" VerticalAlignment="Center"
                            ToolTip="Show a popup reminder when starting a new game while hotkeys are still enabled"
                            ToolTipService.InitialShowDelay="100">
                        <TextBlock Text="i" Foreground="White" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" FontWeight="Bold" FontSize="10" />
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                    <CheckBox Content="Change Default Sound"
                              IsChecked="{Binding IsDefaultSoundChangeEnabled}" />
                    <xctk:IntegerUpDown
                        Margin="5,0,0,0"
                        Value="{Binding DefaultSoundVolume}"
                        TextAlignment="Center"
                        Width="80"
                        Minimum="0"
                        Maximum="10"
                        IsEnabled="{Binding IsDefaultSoundChangeEnabled}"
                        BorderBrush="#3F3F46" />
                    <Border Background="#3498db" CornerRadius="10" Width="12" Height="12"
                            Margin="5,0,0,0" VerticalAlignment="Center"
                            ToolTip="Set the volume that should be used when first launching the game and the game hasn't loaded your preferences"
                            ToolTipService.InitialShowDelay="100">
                        <TextBlock Text="i" Foreground="White" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" FontWeight="Bold" FontSize="10" />
                    </Border>
                </StackPanel>
            </Grid>

            <Separator Margin="0,0,0,10" />
            <TextBlock Text="Hotkey Bindings" FontWeight="Bold" Margin="0,0,0,10" />

            <Grid Margin="0,5,0,0">

                <TabControl Margin="0,5,0,0"
                            Visibility="{Binding Hotkeys.IsOverlayActive, Converter={StaticResource InverseBoolToVisibility}}">
                    <TabItem Header="Player">
                        <ScrollViewer MaxHeight="370" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Player]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Enemies">
                        <ScrollViewer MaxHeight="370" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Enemies]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Target">
                        <ScrollViewer MaxHeight="370" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Target]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                    <TabItem Header="Utility">
                        <ScrollViewer MaxHeight="370" VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Hotkeys[Utility]}"
                                          Padding="5"
                                          ItemTemplate="{StaticResource HotkeyItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="2" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>

                <ScrollViewer MaxHeight="330"
                              Margin="0,40,0,0"
                              VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding Hotkeys.IsOverlayActive, Converter={StaticResource BoolToVisibility}}">
                    <ItemsControl ItemsSource="{Binding Hotkeys.Items}"
                                  Padding="10"
                                  ItemTemplate="{StaticResource HotkeyItemTemplate}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ScrollViewer>
                <TextBox Text="{Binding Hotkeys.SearchText, UpdateSourceTrigger=PropertyChanged}"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Top"
                         Width="90"
                         Height="27"
                         Margin="0,7,5,0">
                    <TextBox.Template>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                                  Focusable="false"
                                                  HorizontalScrollBarVisibility="Hidden"
                                                  VerticalScrollBarVisibility="Hidden"
                                                  VerticalAlignment="Center"
                                                  Margin="5,0,0,0" />
                                    <TextBlock x:Name="placeholderText"
                                               Text="Search..."
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Left"
                                               Margin="5,0,0,0"
                                               Foreground="Gray"
                                               IsHitTestVisible="False">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <MultiDataTrigger>
                                                        <MultiDataTrigger.Conditions>
                                                            <Condition
                                                                Binding="{Binding Path=Text, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                                Value="" />
                                                            <Condition
                                                                Binding="{Binding Path=IsKeyboardFocused, RelativeSource={RelativeSource AncestorType=TextBox}}"
                                                                Value="False" />
                                                        </MultiDataTrigger.Conditions>
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </MultiDataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush"
                                            Value="{StaticResource AccentBrush}" />
                                    <Setter TargetName="border" Property="BorderThickness" Value="1.5" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </TextBox.Template>
                </TextBox>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>
